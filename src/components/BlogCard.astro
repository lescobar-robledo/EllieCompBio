---
const { blogPath, blogTitle } = Astro.props;
---

<section style="margin-top: 2rem;">
  <article style="margin-bottom: 2rem; padding: 1rem; border-radius: 12px; border: 2px solid var(--highlight); background: transparent;">
    <h2><a href={blogPath}>{blogTitle}</a></h2>
    <slot />
  </article>
</section>